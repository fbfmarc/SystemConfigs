import { IAzureNode, IAzureParentTreeItem, IAzureTreeItem } from 'vscode-azureextensionui';
import { SiteClient } from '../SiteClient';
export declare class AppSettingsTreeItem implements IAzureParentTreeItem {
    static contextValue: string;
    readonly label: string;
    readonly childTypeLabel: string;
    readonly contextValue: string;
    private readonly _client;
    private _settings;
    constructor(client: SiteClient);
    readonly id: string;
    readonly iconPath: {
        light: string;
        dark: string;
    };
    hasMoreChildren(): boolean;
    loadMoreChildren(): Promise<IAzureTreeItem[]>;
    editSettingItem(oldKey: string, newKey: string, value: string): Promise<void>;
    deleteSettingItem(key: string): Promise<void>;
    createChild(_node: IAzureNode<AppSettingsTreeItem>, showCreatingNode: (label: string) => void): Promise<IAzureTreeItem>;
    validateNewKeyInput(newKey?: string, oldKey?: string): string | undefined;
}
