import { IAzureNode, IAzureTreeItem } from 'vscode-azureextensionui';
export declare class AppSettingTreeItem implements IAzureTreeItem {
    static contextValue: string;
    readonly contextValue: string;
    private key;
    private value;
    constructor(key: string, value: string);
    readonly id: string;
    readonly label: string;
    readonly iconPath: {
        light: string;
        dark: string;
    };
    edit(node: IAzureNode): Promise<void>;
    rename(node: IAzureNode): Promise<void>;
    deleteTreeItem(node: IAzureNode): Promise<void>;
}
