import { TelemetryProperties } from 'vscode-azureextensionui';
import { SiteClient } from '../SiteClient';
export declare function deploy(client: SiteClient, fsPath: string, configurationSectionName: string, telemetryProperties?: TelemetryProperties): Promise<void>;
