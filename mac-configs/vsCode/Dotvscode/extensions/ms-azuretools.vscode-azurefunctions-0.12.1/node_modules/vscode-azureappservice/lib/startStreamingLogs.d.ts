import * as vscode from 'vscode';
import { SiteClient } from './SiteClient';
export interface ILogStream extends vscode.Disposable {
    isConnected: boolean;
    outputChannel: vscode.OutputChannel;
}
export declare function startStreamingLogs(client: SiteClient, verifyLoggingEnabled: () => Promise<void>, logStreamLabel: string, logsPath?: string): Promise<ILogStream>;
export declare function stopStreamingLogs(client: SiteClient, logsPath?: string): Promise<void>;
