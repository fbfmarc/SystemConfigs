import { User } from 'azure-arm-website/lib/models';
import { SiteClient } from './SiteClient';
/**
 * A local TCP server that forwards all connections to the Kudu tunnel websocket endpoint.
 */
export declare class TunnelProxy {
    private _port;
    private _client;
    private _publishCredential;
    private _server;
    private _openSockets;
    constructor(port: number, client: SiteClient, publishCredential: User);
    startProxy(): Promise<void>;
    dispose(): void;
    private checkTunnelStatus;
    private checkTunnelStatusWithRetry;
    private setupTunnelServer;
}
