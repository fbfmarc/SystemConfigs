import { AzureTreeItem } from 'vscode-azureextensionui';
import { AppSettingsTreeItem } from './AppSettingsTreeItem';
import { ISiteTreeRoot } from './ISiteTreeRoot';
export declare class AppSettingTreeItem extends AzureTreeItem<ISiteTreeRoot> {
    static contextValue: string;
    readonly contextValue: string;
    readonly parent: AppSettingsTreeItem;
    private key;
    private value;
    constructor(parent: AppSettingsTreeItem, key: string, value: string);
    readonly id: string;
    readonly label: string;
    readonly iconPath: {
        light: string;
        dark: string;
    };
    edit(): Promise<void>;
    rename(): Promise<void>;
    deleteTreeItemImpl(): Promise<void>;
}
